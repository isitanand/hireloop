server {
    listen 80;
    root /usr/share/nginx/html;

    # React Router uses client-side routing - unknown paths (e.g. a hard
    # refresh on /dashboard) must fall back to index.html, not 404.
    location / {
        try_files $uri /index.html;
    }
}
